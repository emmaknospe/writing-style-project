# env
.env

# node
frontend/node_modules/
frontend/dist/
frontend/*.tsbuildinfo

# python
api/__pycache__/
api/**/__pycache__/
api/.venv/
.venv/
**/__pycache__/
*.pyc

# corpus: private material is never committed, but the directories must exist
raw/private/*
intermediate/private/*
!raw/private/.gitkeep
!intermediate/private/.gitkeep

# tagging: classifier response cache (rebuildable, ~1MB, churns every run)
tagging/.tagcache.json

# local-only config (machine-specific; see CLAUDE.md)
CLAUDE.local.md
docker-compose.override.yml
.claude

# misc
.DS_Store
