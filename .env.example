# Anthropic
ANTHROPIC_API_KEY=sk-ant-your-key-here
ANTHROPIC_MODEL=claude-sonnet-5

# Qdrant
QDRANT_URL=http://qdrant:6333
QDRANT_COLLECTION_NAME=writing_style
QDRANT_VECTOR_SIZE=1536

# CORS (local `npm run dev` convenience only — not used by the docker-compose/nginx flow)
CORS_ALLOW_ORIGINS=http://localhost:5173,http://localhost:3000

# Gemini embeddings (ingest pipeline -- see ingest/). Auth is via
# Application Default Credentials (`gcloud auth application-default login`,
# or GOOGLE_APPLICATION_CREDENTIALS pointing at a service account key) --
# no API key needed here.
GOOGLE_CLOUD_PROJECT=your-gcp-project-id
GOOGLE_CLOUD_LOCATION=us-central1
GEMINI_EMBEDDING_MODEL=gemini-embedding-001

# RAG retrieval (api/ -- number of corpus chunks the search_corpus tool retrieves per query)
RAG_TOP_K=5

# Corpus classification (tagging/ -- runs on the host, reuses ANTHROPIC_API_KEY).
# Changing the model invalidates tagging/.tagcache.json and forces a full re-tag.
TAGGER_MODEL=claude-haiku-4-5

# Host port mappings
API_PORT=8000
FRONTEND_PORT=3000
QDRANT_HTTP_PORT=6333
QDRANT_GRPC_PORT=6334
